year_input = input("Please input a year.")
year = int(year_input)
leap = []
if year % 4 == 0:
    if year % 100 == 0:
        if year % 400 == 0:
            leap = True
        else:
            leap = False
    else:
        leap = True
else:
    leap = False

print(leap)
